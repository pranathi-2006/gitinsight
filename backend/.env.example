# GitHub Personal Access Token (PAT)
# Generate one at https://github.com/settings/tokens
# Ensure the token has necessary permissions (e.g. read access to public repos)
GITHUB_TOKEN=your_github_token_here
